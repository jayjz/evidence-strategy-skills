# Strategy Framework References

This directory contains reusable research and references for strategy frameworks used by decision-oriented skills.

Framework presence does not imply a standalone skill should exist.

Do not create executable framework abstractions unless repeated implementation needs justify them.
